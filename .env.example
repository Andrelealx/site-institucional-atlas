# Atlas Tecnologias — variáveis de ambiente (copie para .env e preencha).
# NUNCA comite o .env com valores reais (já está no .gitignore).

# Meta Pixel / dataset — Events Manager → Connect Data → Web.
# Em 2026 o Meta chama o Pixel de "dataset"; o ID é o mesmo.
# Vazio = o Pixel NÃO carrega (fica só a fundação de consentimento pronta).
META_DATASET_ID=

# WhatsApp de destino em E.164 (só dígitos: 55 + DDD + número).
WHATSAPP_E164=5521992565057
